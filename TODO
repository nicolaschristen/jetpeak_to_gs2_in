-- Can the database be spread around ?

-- Make file reading more flexible (no 'name = val' required)

-- When running pick_shots/plot_check_TRANSP, why is the magnitude of the magnetic flux different in EFIT from TRANSP ?
